# WAB / SDL 解像度追従修正レポート

## 概要

今回の対応では、2 つの異なる問題を段階的に修正しました。

1. 初期の表示乱れ（横方向に周期的に繰り返す崩れ、VRAM の転送崩れ）
2. その後の解像度追従不良（WAB 解像度変更時に SDL ホスト window が追従しない問題）

初期の表示乱れは、WAB/Cirrus の最終出力転送と描画境界の扱いが不正だったことが原因でした。後段の修正では、ホストのサイズ同期がずれていたことが原因でした。

特に後段の問題としては以下がありました。

- ゲスト側の表示サイズが変化したとき、SDL 側の window size 更新が追従しない
- 解像度が大きくなる場合と小さくなる場合で、更新判定が非対称になっていた
- width と height を個別に更新する経路で、古い値が混ざってしまい、最終サイズとズレることがあった

---

## 修正内容

### 0. 初期の表示乱れ修正

対象: [wab/cirrus_vga.c](wab/cirrus_vga.c), [wab/wab.c](wab/wab.c), [sdl/scrnmng.c](sdl/scrnmng.c)

- WAB から SDL への転送処理を、1 行単位で安全にコピーする形に整理
- 実表示幅・高さと VRAM の線幅の扱いを実データに合わせて補正
- 行ごとのバイト数とコピー範囲が実際の表示サイズを超えないように制御
- これにより、横方向に周期的に繰り返す画面崩れを抑制

---

## 修正内容（続き）

### 1. WAB の実表示解像度を SDL に反映する処理を整理

対象: [wab/wab.c](wab/wab.c)

- `np2wab.realWidth` / `np2wab.realHeight` の変化時に、SDL 側のサイズ更新を実行する条件を明確化
- relay が有効な状態で、表示サイズが現在の `scrnmng` と一致していない場合に、即座にサイズ更新を行うよう修正
- 解像度が下がる場合でも追従できるよう、過去の last 値を更新するタイミングを整理

### 2. SDL の resize 処理で width / height の同期ズレを防止

対象: [sdl/scrnmng.c](sdl/scrnmng.c)

- `scrnmng_setsize()` の更新経路を見直し、最終サイズを正しく反映するよう修正
- width と height の個別更新が古い値を参照してしまうケースを避け、両方を同時に整合させる形に整理
- これにより、解像度の拡大と縮小の両方で動作が安定

### 3. 実解像度の更新タイミングを調整

対象: [wab/wab.c](wab/wab.c)

- 画面サイズが変更された直後に `ga_lastrealwidth` / `ga_lastrealheight` を更新するようにした
- これで、次の描画時に「縮小後の解像度が未反映」になる状況を防止

---

## 影響範囲

今回の修正は以下の挙動に効きました。

- WAB / Cirrus VGA の解像度切替時に、ホストの SDL window が追従する
- 画面の収まりが崩れず、Win3.1 の表示がはみ出さない
- 解像度が大きくなった時だけでなく、小さくなった時も同期が維持される

---

## 検証結果

ビルド確認は以下で実施済みです。

- 実行コマンド: `cd /home/daisuke/Repos/NP2kai/build_sdl2 && make -j4`
- 結果: `Built target sdlnp21kai_sdl2`

つまり、今回の修正を含むコードは、コンパイル上は正常に通過しています。

---

## 補足

この報告では、ログ削除の件は含めていません。
今回の修正は、表示解像度と SDL window サイズの整合性を修正する作業に集中しています。
